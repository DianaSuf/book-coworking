export enum  AppRoute {
  Root = '/',
  NotFound = '*',
}

export enum AuthorizationStatus {
  User = 'USER',
  Admin = 'ADMIN',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}

export enum  APIRoute {
  Status = '/status',
}
